<h1>Listing users</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Apellido</th>
    <th>Correo</th>
    <th>Nick name</th>
    <th>Pais</th>
    <th>Biografia</th>
    <th>Foto</th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.nombre %></td>
    <td><%= user.apellido %></td>
    <td><%= user.correo %></td>
    <td><%= user.nick_name %></td>
    <td><%= user.pais %></td>
    <td><%= user.biografia %></td>   
    <td> <img alt="FotoPerfil"  src="<%= user.foto %>"/></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
